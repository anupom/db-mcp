# Required — shared with Cube.js (must match docker-compose.yml)
CUBE_JWT_SECRET=this-is-a-very-long-secret-for-development-only-32-chars
CUBE_API_URL=http://localhost:4000/cubejs-api/v1

# Infrastructure — default database (used by auto-initialization)
POSTGRES_HOST=localhost
POSTGRES_PORT=5434
POSTGRES_DB=ecom
POSTGRES_USER=cube
POSTGRES_PASSWORD=cube

# Optional — AI features
# ANTHROPIC_API_KEY=sk-ant-...

# Optional — encrypt stored database credentials (min 32 chars)
# ADMIN_SECRET=your-admin-secret-key-here-at-least-32-chars

# Optional — Clerk authentication (SaaS mode)
# When both are set, auth is enabled and the app runs in multi-tenant SaaS mode
# CLERK_SECRET_KEY=sk_test_...
# CLERK_PUBLISHABLE_KEY=pk_test_...
